{> "views/header.dust"/}

<p>voici a quoi ressemble le fichier {req.params.file} du repo "{req.params.repo}" appartenant a {req.params.user}</p>
<p>en fait yaura un truc different en fonction du type de fichier</p>

{> "views/footer.dust"/}